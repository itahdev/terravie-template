includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app

    # The level 2 is the highest level
    level: 2

    #ignoreErrors:
        #- '#Unsafe usage of new static#'

    excludePaths:
        - tests
        - app/Securities

    checkMissingIterableValueType: false
